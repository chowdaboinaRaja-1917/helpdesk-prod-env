import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.exampleapp',
  appName: 'HelpDesk',
  webDir: 'dist/userhelpdesk/browser'
};

export default config;
